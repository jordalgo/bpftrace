NAME it mutates the passed variable
PROG config = { unstable_macro=1; } macro set($x) { $x += 1; $x } BEGIN { $a = 1; set($a); print($a); exit(); }
EXPECT 2
TIMEOUT 1

NAME it does not mutate previously passed variable
PROG config = { unstable_macro=1; } macro set($x) { $x += 1; $x } BEGIN { $a = 1; set($a); set(1); print($a); exit(); }
EXPECT 2
TIMEOUT 1

